import React from 'react';
import HomeScreen from './index'; // Adjust the path based on your project structure

const AppNavigator: React.FC = () => {
  return (
    <HomeScreen />
  );
}

export default AppNavigator;
